# datastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build datastructure` to build the library.

## Running unit tests

Run `nx test datastructure` to execute the unit tests via [Jest](https://jestjs.io).
